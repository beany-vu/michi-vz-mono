node_modules/
.npmrc
dist/
# Local env files (may hold the GA_MEASUREMENT_ID etc.) - never commit
.env
.env.*
!.env.example
.turbo/
coverage/
*.log
*.tsbuildinfo
.DS_Store
# Playwright MCP scratch (screenshots / snapshots / console logs)
.playwright-mcp/
**/.vitepress/cache/
**/.vitepress/dist/
storybook-static/
playground/screenshot.png
# pnpm store written into the repo by the docker compose containers (root-owned)
.pnpm-store/

# AI assistant artifacts - keep ALL of these out of git (user preference)
.claude/
CLAUDE.md
CLAUDE.local.md
.cursor/
.cursorrules
.aider*
.github/copilot-instructions.md
.windsurfrules
