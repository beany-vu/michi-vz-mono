---
"@michi-vz/wc": patch
"@michi-vz/core": patch
---

wc: point the CDN default entry (jsdelivr/unpkg) at the self-contained browser bundle so the bare package URL works in a module script. core: clean up public prop JSDoc (stale fountain defaults, internal porting notes, clearer joinBy/positionMode/crosshair wording).
