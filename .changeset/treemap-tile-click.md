---
"@michi-vz/core": minor
"@michi-vz/wc": minor
---

TreemapChart `onTileClick(leaf, event)`: a leaf-tile click callback carrying the same `TreemapLeafContext` the tooltip formatter receives, on the SVG, canvas and webgpu renderers. The web component dispatches it as `michi-vz:tileclick`.
