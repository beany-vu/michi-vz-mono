---
"@michi-vz/core": minor
---

ScatterChart `dScaleLegend.title` now also accepts an array of strings, rendered as stacked lines above the size arcs. Lets long translated titles wrap instead of spilling past the chart's right edge.
